const Discord = require('discord.js');


module.exports = {
  name: 'ping',
  value: "N/A",
  description: "TEST if the bot is ok",
  options: [],
  execute(interaction, client, callback) {
      console.log("CheckID slashed")
  }
}